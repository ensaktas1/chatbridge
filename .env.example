# Provision a Neon Postgres database through the Vercel Marketplace.
DATABASE_URL=postgresql://user:password@host/database?sslmode=require

# A long random value. Vercel sends it to the daily cleanup cron as a Bearer token.
CRON_SECRET=replace-with-a-long-random-value
